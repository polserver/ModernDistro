use uo; 
use os; 

program fseffect(who, what, lastx, lasty, lastz) 

    lastx := lastx;
    lasty := lasty;
    lastz := lastz;
    
    PlayObjectCenteredEffect(who, 0x3709, 0x0a, 0x1e ); 
    PlaySoundEffect(who,0xf8); 

endprogram 
