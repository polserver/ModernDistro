//
// .logoff - disconnects the user from the server
//

use uo;
use os;

program textcmd_logoff( who )
    SendSysMessage(who, "You will be logged off in 5 seconds...", 2, 33 );
    Sleep(5);
    DisconnectClient(who);
endprogram
