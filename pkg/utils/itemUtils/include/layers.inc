const LAYER_NONE		:=  0x0;   // Not used?
const LAYER_HAND1		:=  0x1;   // One handed weapon.
const LAYER_HAND2		:=  0x2;   // Two handed weapon, shield or misc.
const LAYER_SHOES		:=  0x3;   // Shoes.
const LAYER_PANTS		:=  0x4;   // Pants.
const LAYER_SHIRT		:=  0x5;   // Shirt.
const LAYER_HELM		:=  0x6;   // Helm or Hat.
const LAYER_GLOVES		:=  0x7;   // Gloves.
const LAYER_RING		:=  0x8;   // Ring.
const LAYER_TALISMAN	:=  0x9;   // Unused.
const LAYER_NECK		:=  0xA;   // Neck.
const LAYER_HAIR		:=  0xB;   // Hair
const LAYER_WAIST		:=  0xC;   // Waist (half apron).
const LAYER_CHEST		:=  0xD;   // Torso (inner) (chest armor).
const LAYER_WRIST		:=  0xE;   // Bracelet.
const LAYER_PACK2		:=  0xF;   // Unused (backpack, but ord. bp is 0x15).
const LAYER_BEARD		:=  0x10;   // Facial hair.
const LAYER_TUNIC		:=  0x11;   // Torso (middle) (tunic, sash etc.).
const LAYER_EARS		:=  0x12;   // Earrings.
const LAYER_ARMS		:=  0x13;   // Arms.
const LAYER_CAPE		:=  0x14;   // Back (cloak).
const LAYER_PACK		:=  0x15;   // Backpack
const LAYER_ROBE		:=  0x16;   // Torso (outer) (robe)
const LAYER_SKIRT		:=  0x17;   // Legs (outer) (skirt/robe).
const LAYER_LEGS		:=  0x18;   // Legs (inner) (leg armor).
const LAYER_MOUNT		:=  0x19;   // Mount (horse, ostard etc.).
const LAYER_VENDORSTOCK	:=  0x1A;   // This vendor will sell and restock.
const LAYER_VENDOREXTRA	:=  0x1B;   // This vendor will resell to players but not restock.
const LAYER_VENDORBUY	:=  0x1C;   // This vendor can buy from players but does not stock.
const LAYER_BANKBOX		:=  0x1D;   // Contents of bankbox
const LAYER_RESERVED	:=	0x1E;	// Unused, using this layer makes you invisible to other players. Strange.
const LAYER_TRADE		:=  0x1F;   // Can be multiple of these, do not use directly.
