/*
        Maintained by The POL Distro Team

        2018-07-25
*/
use uo;
use os;

program onRemove( mobile, container, item, item_amount, movetype )
	mobile := mobile;
	container := container;
	item := item;
	item_amount := item_amount;
	movetype := movetype;
	//To-do
	//Play sound effect now that item is being removed.

	return 1;
endprogram
